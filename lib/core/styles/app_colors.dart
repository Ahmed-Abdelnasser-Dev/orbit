import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFF762ED5);

  // Neutrals and text
  static const Color backgroundDark = Color(0xFF1A1A1A);
  static const Color bodyText = Color(0xFFB1ADA1);
  static const Color whiteSoft = Color(0xFFF4F3EE);

  // Derived
  static const Color onPrimaryDark = whiteSoft;
  static const Color onPrimaryLight = whiteSoft;
}
